{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "ignorePatterns": ["**/routeTree.gen.ts", "**/base/*.tsx"],
  "rules": {
    "typescript/no-explicit-any": "error",
    "typescript/no-floating-promises": "allow", // TODO: Enable this rule and fix all issues
    "typescript/restrict-template-expressions": "allow" // TODO: Enable this rule and fix all issues
  }
}
