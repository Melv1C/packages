---
title: Quick Start
description: Get up and running with @melv1c/ui-core in under 5 minutes.
icon: Zap
---

## Requirements

- **React** 19 or later
- **Tailwind CSS** v4 or later
- **tw-animate-css** v1.4 or later

## Install the Package

```package-install
npm i @melv1c/ui-core
```

## Install Peer Dependencies

@melv1c/ui-core requires Tailwind CSS v4 and tw-animate-css as peer dependencies:

```package-install
npm i tailwindcss@^4 tw-animate-css@^1.4
```

## Configure CSS

Import the base styles in your main CSS file. You need Tailwind, the animation library, the `@source` directive so Tailwind can scan the package's classes, the base CSS, **and** a theme:

```css title="app.css"
@import 'tailwindcss';
@import 'tw-animate-css';

@source "../node_modules/@melv1c/ui-core";

/* Import base theme structure */
@import '@melv1c/ui-core/base.css';

/* Import a theme (choose one) */
@import '@melv1c/ui-core/themes/default.css';
```

### Available Themes

| Theme   | Import                               |
| ------- | ------------------------------------ |
| Default | `@melv1c/ui-core/themes/default.css` |
| Claude  | `@melv1c/ui-core/themes/claude.css`  |
| Vercel  | `@melv1c/ui-core/themes/vercel.css`  |
| Twitter | `@melv1c/ui-core/themes/twitter.css` |
| T3Chat  | `@melv1c/ui-core/themes/t3chat.css`  |
| Doom64  | `@melv1c/ui-core/themes/doom64.css`  |

See the [Theming guide](/docs/ui-core/theming) for more details on customizing themes.

## Set Up the Provider

Wrap your application with `UICoreProvider`. This is **required** — all @melv1c/ui-core components expect to be rendered inside the provider for i18n, toasts, and internal context to work correctly.

```tsx title="app.tsx"
import { UICoreProvider } from '@melv1c/ui-core';

export function App({ children }: { children: React.ReactNode }) {
  return (
    <UICoreProvider i18nConfig={{ locale: 'en' }} hasToaster>
      {children}
    </UICoreProvider>
  );
}
```

See the [Providers guide](/docs/ui-core/providers) for all configuration options.

## Verify Installation

Try importing a component to verify everything is set up correctly:

```tsx title="test.tsx"
import { Button } from '@melv1c/ui-core';

export function TestButton() {
  return (
    <Button variant="default" size="default">
      It works!
    </Button>
  );
}
```

If you see a styled button, you're all set!

## Next Steps

<Cards>
  <Card title="Providers" href="/docs/ui-core/providers">
    Learn about UICoreProvider configuration options.
  </Card>
  <Card title="Components" href="/docs/ui-core/components">
    Browse all available components.
  </Card>
</Cards>
