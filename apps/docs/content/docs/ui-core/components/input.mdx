---
title: Input
description: Displays a form input field or a component that looks like an input field.
---

<ComponentPreview name="input-demo" />

## Usage

```tsx
import { Input } from '@melv1c/ui-core';
```

```tsx
<Input type="text" placeholder="Placeholder" />
```

## Examples

### Basic

A basic text input.

<ComponentPreview name="input-basic" />

### Disabled

Use the `disabled` prop to prevent user interaction.

<ComponentPreview name="input-disabled" />

### Invalid

Use the `aria-invalid` prop to show an error state.

<ComponentPreview name="input-invalid" />

### Required

Use the `required` prop to mark the input as required.

<ComponentPreview name="input-required" />

### File

Use `type="file"` for file uploads.

<ComponentPreview name="input-file" />

### Inline

Display an label and input on the same line.

<ComponentPreview name="input-inline" />

### With Badge

Display a badge inside the input for tags or tokens.

<ComponentPreview name="input-badge" />

### With Field

Wrap the input in a `Field` component for label, description, and error message support.

<ComponentPreview name="input-field" />

### Input Group

Use `InputGroup`, `InputGroupInput`, and `InputGroupAddon` to create composite inputs with prefixes, suffixes, icons, or buttons.

<ComponentPreview name="input-group-demo" />

## API Reference

See the [Shadcn UI](https://ui.shadcn.com/docs/components/input) documentation for more information.
